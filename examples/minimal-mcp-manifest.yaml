# Minimal example - free MCP server with no payments
version: "0.1"
name: "open-translator-mcp"
description: "Free translation tools via MCP"

mcp:
  server_url: "https://translator.example.com/mcp"
  transport: "http"

# No payment section = free to use